name: memory-sanitize gate rules

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run the mechanically decidable memory-sanitize rules
        run: ./scripts/check-gate.sh
