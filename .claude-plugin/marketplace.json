{
  "name": "lacolaco-plugins",
  "owner": {
    "name": "lacolaco"
  },
  "description": "Claude Code plugins by lacolaco",
  "plugins": [
    {
      "name": "protect-main-branch",
      "description": "Prevent git operations that would modify the main branch (or configured branches) in Claude Code. Individual subcommands can be opted out via PROTECT_MAIN_BRANCH_ALLOWED_SUBCOMMANDS, or the hook can be turned off entirely for the current scope via PROTECT_MAIN_BRANCH_DISABLE.",
      "version": "2.2.0",
      "source": "./protect-main-branch"
    },
    {
      "name": "session-handover",
      "description": "Reincarnation-style handover/takeover for Claude Code. Each agent self-names with an English first name and keeps an identity-scoped document at <base>/<name>.md, with a YAML frontmatter `description` field (the worker's stable job description) followed by a curated Knowledge block (stock) and an append-only History block (flow). The frontmatter lets `/takeover` list candidates by job description without reading the body. <base> is resolved deterministically by the bundled `handover-dir` command (on the Bash tool's PATH while the plugin is enabled), which walks up from cwd to the nearest .handover/ directory and falls back to $HOME/.handover when none is found. v2.x users are auto-migrated: when `handover-dir` finds a legacy `.claude/handover/` on the walk-up path, it lifts it to `.handover/` at the same level on first use. v3.0.x documents without frontmatter are still selectable in `/takeover` (description rendered as `(no description)`) and gain a `description` field the next time their owner runs `/handover`. A successor takes over an identity to inherit its memory, understanding, and experience.",
      "version": "3.1.0",
      "source": "./session-handover"
    },
    {
      "name": "retrospective",
      "description": "Retrospective skill based on a 6-stage process (input → interpretation → planning → action → inspection → output) to surface problems and apply improvements",
      "version": "1.0.0",
      "source": "./retrospective"
    },
    {
      "name": "session-tts",
      "description": "Read Claude Code responses aloud locally with a different Japanese voice per session. Instructs Claude to deliver mid-turn progress narration via a synchronous Bash call into the say adapter, plus a TodoWrite hook that nudges narration at task transitions. Permission prompts include the workspace name. ON by default; playback volume is adjustable via /session-tts:volume. Engine and models are managed automatically. Apple Silicon only.",
      "version": "0.11.1",
      "source": "./session-tts"
    }
  ]
}
